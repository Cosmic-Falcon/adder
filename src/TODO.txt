TODO

Body:
	

Create Entity class:
	* Effectively wrapper around Body
	* Should allow a user-overridable function that can modify some behavior

Create Brain class:
	* Manages interactions between entities
	* Users should be able to add entities to the object
		*hash table and some sorta hashing system